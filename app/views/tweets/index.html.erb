<%= content_tag :h1 do %>
  <%= image_tag @tweets.first.user.profile_image_url %>
  @<%= @handle %> Latest Tweets
<% end %>

<%= render 'search' %>

<ul class="tweets">
  <%= render partial: 'tweet', collection: @tweets, as: :tweet %>
</ul>
